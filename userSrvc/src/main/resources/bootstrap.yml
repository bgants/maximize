spring:
  application:
    name: userservice
  profiles:
    active: native
  cloud:
    config:
      name: userservice
      enabled: true
      uri: http://localhost:8888/
      failFast: true
      retry:
        maxAttempts: 20